# Copy to /etc/project-kagya/backend.env and restrict permissions to the service user.
KAGYA_CONFIG_PATH=/opt/project-kagya/config.yaml
KAGYA_ADMIN_TOKEN=replace-with-long-random-token

# Optional when model access requires authentication.
# HF_TOKEN=replace-with-huggingface-token
