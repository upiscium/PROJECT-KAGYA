# TODO

- Improve real-model prompt/template design for Gemma 4 after frontend milestone. Current runtime prevents prompt/debug leakage, but response quality still needs a model-specific prompt format or chat-template strategy.
